import React from "react";

const Overlay = () => {
  return (
    <div className="overlay invisible fixed inset-0 z-40 bg-black bg-opacity-10 opacity-0"></div>
  );
};

export default Overlay;
